<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>

        <!-- Disable directory listing -->
        <directoryBrowse enabled="false" />

    </system.webServer>
</configuration>
